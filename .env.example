# Set locally. Do not commit .env or paste credentials into chat.
DATABASE_URL=
PREVIEW_WORKER_TOKEN=
# Optional overrides; defaults are documented in README.md.
HOST=
PORT=
PREVIEW_WORKER_URL=
PREVIEW_PUBLIC_URL=
PREVIEW_HOST=
PREVIEW_PORT=
# Docker Compose database provisioning only:
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
